venv/
__pycache__/
*.pyc
.env
node_modules/
dist/
build/
.idea/
*.log

# Generated by frontend/dashboard/build_dashboard.py + splice_dashboard.py --
# regenerate from the live DB rather than committing a stale data snapshot.
frontend/dashboard/dashboard_data.json
frontend/dashboard/dashboard_final.html

# Runtime logs from scripts/nightly_refresh.ps1
scripts/logs/

# Generated by backend/backtest_projections.py -- regenerate from the live
# DB rather than committing a snapshot that goes stale as more seasons arrive.
backend/backtest_results.csv

# Local cache of MLB headshot images (see fetch_headshots.py) -- binary
# blobs, not source; regenerates itself from MLB's own CDN on first use.
frontend/dashboard/headshots_cache/
